version: "3.1"
services:
  rasa:
    image: rasa/rasa
    ports:
      - 5005:5005
    volumes:
      - ./:/app
    command:
      - run
  action-server:
    image: rasa/rasa-sdk
    volumes:
      - ./actions:/app/actions
    ports:
      - 5055:5055
